apiVersion: v1
kind: Endpoints
metadata:
  name: health
  namespace: app-kid
---
# Health endpoint pour le backend Django
# À ajouter dans views.py:
#
# from django.http import JsonResponse
# 
# def health_check(request):
#     return JsonResponse({
#         'status': 'healthy',
#         'service': 'kid-livraison-backend',
#         'version': '1.0.0'
#     })
#
# Dans urls.py:
# path('api/health/', views.health_check, name='health'),
